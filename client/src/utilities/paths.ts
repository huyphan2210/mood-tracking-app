enum PATH {
  HOME = "/",
  LOGIN = "/authentication/login",
  SIGNUP = "/authentication/sign-up",
}

export const AUTHENTICATION_PATHS: string[] = [
  PATH.HOME,
  PATH.LOGIN,
  PATH.SIGNUP,
];

export default PATH;
